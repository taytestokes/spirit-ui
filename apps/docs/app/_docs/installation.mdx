import { Snippet } from "../_components/snippet/snippet";

# Installation

Get started by first installing the theme package to get access to the core Spirit UI stylesheet that will provide the Inter font and style variables that can be reused.

<Snippet>npm install @spirit-ui/theme</Snippet>

After installing the package, import the the stylesheet as early as possible into the application.
