import { SpacingPalette } from "../_components/spacing-palette/spacing-palette";

# Spacing

Spirit UI includes a predefined set of values that helps to simplify the creation of component and page layouts.

<SpacingPalette />
