import { Button } from "./button";

const meta = {
  title: "Button",
  component: Button,
};

export default meta;

export const Primary = {
  args: {
    appName: "Button",
    children: "I am a primary button.",
  },
};
